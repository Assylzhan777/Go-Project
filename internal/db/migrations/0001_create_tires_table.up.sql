CREATE TABLE tires (
                       id SERIAL PRIMARY KEY,
                       brand TEXT NOT NULL,
                       size TEXT NOT NULL,
                       price number NOT NULL
);
